#include<stdio.h>
#include<conio.h>
int main()
{
int a,b,area;
printf("\nEnter the height and base of the triangel");
scanf("%d",&a);
scanf("\n%d",&b);
area=0.5*a*b;
printf("the area of the triangle is %d",area);
return 0;
}
